@model IEnumerable<HK.Models.Exporter>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ExporterName)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.ExporterAddress)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ExporterName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.ExporterAddress)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.ExporterID }) |
                @Html.ActionLink("Details", "Details", new { id=item.ExporterID }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.ExporterID })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>
